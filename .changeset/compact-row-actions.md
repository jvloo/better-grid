---
"@better-grid/pro": patch
---

Keep row action triggers centered in compact columns by removing inherited cell padding from action cells, and add a `triggerSize` option for tighter table layouts.
