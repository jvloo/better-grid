const opts = { // @better-grid/migrate: review — 'summaryRows' -> 'pinned.bottom' (nested rename — needs object grouping)
summaryRows: bots, // @better-grid/migrate: review — 'topSummaryRows' -> 'pinned.top' (nested rename — needs object grouping)
topSummaryRows: tops };
