const opts = { // @better-grid/migrate: review — 'fixedColumnsLeft' -> 'frozen.left' (nested rename — needs object grouping)
fixedColumnsLeft: 2, // @better-grid/migrate: review — 'fixedRowsTop' -> 'frozen.top' (nested rename — needs object grouping)
fixedRowsTop: 1, // @better-grid/migrate: review — 'fixedRowsBottom' -> 'pinned.bottom' (nested rename — needs object grouping)
fixedRowsBottom: 1 };
